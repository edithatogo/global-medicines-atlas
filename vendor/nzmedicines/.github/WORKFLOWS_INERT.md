# Inert historical workflows

The adjacent workflow files are preserved as part of the imported
`nzmedicines` history. They are not executable workflows for
`global-medicines-atlas`, are deliberately excluded from root workflow
validation, and must not be copied into `.github/workflows/` without a fresh
security and supply-chain review.
