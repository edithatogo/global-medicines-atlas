# Remaining federation implementation: 2026-09-05

This local audit uses checkout `19f86225ae171176b047c129cdceea90d705c1ec`.
It corrects the conversational claim that all remaining work is externally
gated. It does not establish current GitHub or Hugging Face state. The browser
could not verify its administrative policy; no alternative transport was used
to circumvent that refusal.

## Executable implementation queue

| Track | Repository evidence | Remaining implementation |
| --- | --- | --- |
| Platinum | Phase 2 distinguishes completed dataset identity from unchecked product queries | Shared MBS/PBS query services, CLI/API routes, deterministic pagination and evidence envelopes; then history and accessible atlas views |
| Frontier | Phase 2 observation models precede unchecked benchmarks | Executable bounded query benchmarks, measured profiles and dispositions |
| Frontier | Phases 3–5 remain unchecked | Federation-aware catalogue experiments, cross-dataset research packages and deterministic Gold graph exporters |
| Australian Silver/Gold | Source containment graphs and native comparison foundations exist | Canonical medicine/organization projections, reviewed relationships and producer/schema-profile integration |
| Public data plane | Source metadata validators and synthetic admission fixtures exist | Hosted metadata append, real producer/admission/distribution integration and exact collection mutation inputs |
| Donor consolidation | History authority envelope precedes hosted append task | Append-only hosted transaction with parent-revision comparison and preservation verification |

Synthetic fixtures can establish implementation behavior. They cannot establish
real publication, source completeness or qualification. In particular,
`quality/qualifications/australian-benefits-synthetic-producer.json` declares
`publishable: false` and cannot supply a real publication manifest.

## Existing authorization and remaining decisions

[Decision 0009](../../conductor/decisions/0009-australian-health-authority-and-public-data-plane.md)
records the maintainer's scoped Australian redistribution/publication approval.
The exact legacy authorization receipt and existing track publication records
must be used before claiming a new approval is needed. They still require exact
source, destination and digest bindings for hosted execution. Source-specific
rights for unenumerated vocabulary bytes, independent recovery arrangements,
donor archival and stable production release remain separate decisions.

The existing PBS publication workflow replaces target contents. A metadata-only
update must use an append transaction with a pinned parent revision, explicit
new-object manifest, sibling preservation checks and anonymous verification.
Generic continuation is not a reason to rerun a replacement publisher.

## Evidence limits and delivery

The checkout context validator passed. This is a concrete counterexample audit,
not full track qualification. No plan checkbox is completed by this document.
An open PR with `mergeStateStatus=BLOCKED` does not establish a merge queue,
healthy protection configuration or the cause of the block. Those claims need
current rule and check evidence when browser access is restored.
