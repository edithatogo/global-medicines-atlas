---
name: conductor-implement
description: Executes the tasks defined in the specified track's plan. Use this to start or continue working on a feature, bug fix, or chore.
---

# Conductor Implement

Load and follow the pinned upstream protocol at
`.agents/plugins/conductor/skills/conductor-implement/SKILL.md`.

This repository vendors that plugin as a Git submodule. Do not copy the
protocol into this wrapper. If the submodule is missing, run
`git submodule update --init --recursive`.
