---
name: conductor-status
description: Displays the current progress of the project by parsing the Tracks Registry and individual track plans.
---

# Conductor Status

Load and follow the pinned upstream protocol at
`.agents/plugins/conductor/skills/conductor-status/SKILL.md`.

This repository vendors that plugin as a Git submodule. Do not copy the
protocol into this wrapper. If the submodule is missing, run
`git submodule update --init --recursive`.
