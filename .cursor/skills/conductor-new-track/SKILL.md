---
name: conductor-new-track
description: Plans a new track (feature or bug fix), generates spec/plan documents, and updates the registry.
---

# Conductor New Track

Load and follow the pinned upstream protocol at
`.agents/plugins/conductor/skills/conductor-new-track/SKILL.md`.

This repository vendors that plugin as a Git submodule. Do not copy the
protocol into this wrapper. If the submodule is missing, run
`git submodule update --init --recursive`.
