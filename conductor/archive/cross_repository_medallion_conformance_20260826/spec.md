# Specification

Adopt the byte-versioned medallion v1 JSON Schema vocabulary shared with
`reimbursement-atlas` and `archive-govt-nz`. Keep local runtime models and
validate layer names, checksums, lineage, rights, promotion status, complete
gates, adjacent transitions, and fail-closed behavior in CI.
